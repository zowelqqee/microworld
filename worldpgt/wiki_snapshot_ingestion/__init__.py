"""Offline self-ingestion helpers for local Wikipedia snapshots.

This package never fetches from the network and never writes accepted or
promoted memory. It prepares proposals and dry-run regression artifacts only.
"""

